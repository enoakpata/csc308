#include <stdio.h>
int main() {
    int i = 0;
    for (i; i < 5; i++) {
        printf("%d", i);
    }

    int flag = 1;
    while (flag == 1)
    {
        printf("Hello, world!\n");
    }

    return 0;
}